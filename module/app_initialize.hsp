
// 起動時処理
#module
// 起動時に一度だけ行う処理
#deffunc AppInitialize

	// 多重起動防止
	aplsel@ appTitle
	if(stat != true){
		end
	}

	// タスクトレイ
	SetTrayIconFile tasktrayIcon
	CreateTrayIcon appTitle, 0, 0

	// タスクトレイのメニューを設定
	CreateTasktrayMenu

	// ウィンドウ設定
	gsel 0,isDebug - 1
	title appTitle

	// ディレクトリの作成
	MakeDirectory "./note"
	MakeDirectory "./memo"
	MakeDirectory "./trash"

	return
#global